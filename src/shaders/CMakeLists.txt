# Shaders
#add_custom_command(
#        TARGET ${PROJECT_NAME} POST_BUILD
#        COMMAND ${CMAKE_COMMAND} -E copy_directory
#        ${SRC_DIR}/shaders
#        ${CMAKE_CURRENT_BINARY_DIR}/shaders)
#set(SOURCES frag.spv vert.spv)
